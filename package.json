{
  "name": "hacienda",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev:game1",
    "dev:game1": "vite",
    "build": "npm run build:game1",
    "build:game1": "tsc --noEmit && vite build && node scripts/publish-pages.mjs arma-el-presupuesto-de-el-salvador",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "4.2.0"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vite": "8.1.0"
  }
}
